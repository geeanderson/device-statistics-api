# ClusterIP — reachable only within the cluster, no external exposure.
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: device-statistics
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
