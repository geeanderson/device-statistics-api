# .env.example
# Template for environment variables used by the application.

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=devicedb
DB_USER=postgres
DB_PASSWORD=changeme

# Service URL
DEVICE_API_URL=http://localhost:8001   # Internal URL of the Device Registration API
